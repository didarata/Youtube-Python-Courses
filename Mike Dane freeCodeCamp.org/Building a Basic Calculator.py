num1 = input("Enter a number: ")                   #BY DEFAULT PYTHON CONVERTS NUMBER TO A STRING
num2 = input("Enter another number: ")             #Integer number is a WHOLE NUMBER, int function converts string to number but only for WHOLE NUMBERS.
results = int(num1) + float(num2)                  #Float is a number that has decimal (ex 3.7). This are the two ways to conver input from user to numbers again  which has to be done in order for math funcions to work.
print(results)
